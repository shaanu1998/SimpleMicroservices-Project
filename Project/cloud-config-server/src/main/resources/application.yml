server:
  port:
    9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https:https://github.com/shaanu1998/config-server
        clone-on-start: true